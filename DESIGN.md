# DESIGN.md — Fall Down (2D-Fall)

세로형(480×800) 하강 액션 게임. NS-Shaft("아래로 아래로") 스타일로, 플레이어는 발판을 밟으며 끝없이 아래로 내려간다. 깊이 내려갈수록 난이도와 화면 분위기가 함께 어두워진다.

## 1. 핵심 루프

떨어진다 → 발판에 착지한다(HP +1) → 함정·몬스터를 피해 다음 구멍/발판으로 이동한다 → 더 깊이 떨어진다 → 죽으면 점수(깊이 + 코인)를 기록한다.

## 2. 게임 모드

### INFINITE (무한 모드)
- 화면이 **자동으로 아래로 스크롤**되며, 깊이에 따라 스크롤 속도가 32→160px/s까지 가속된다.
- 화면 상단에 **천장 가시**가 있다. 닿으면 데미지, 완전히 밀려 올라가면 즉사.
- 발판은 공중에 떠 있는 얇은 바(3~6타일). 깊어질수록 좁아지고 간격이 벌어진다.
- 점프 없음. 좌우 이동만으로 낙하를 제어한다.

### CLASSIC (기본 모드)
- 자동 스크롤 없음. 카메라는 플레이어가 내려간 만큼만 따라온다.
- 벽에서 벽까지 이어지는 **바닥층**(내부 13타일)에 **구멍 1~2개**만 뚫려 있다. 구멍이 유일한 하강 루트다.
- **점프 가능** (SPACE/↑/W, 높이 약 46px ≈ 3타일). 가시·슬라임을 뛰어넘어 구멍까지 이동한다.
- **얼음 블럭**: 밟으면 조작이 관성으로 처리되어 미끄러진다 (`ICE_RESPONSE = 2.6`).
- 이동속도·낙하속도가 무한 모드보다 느리다 (정밀 플랫포밍 지향).

## 3. 공통 시스템

| 시스템 | 규칙 |
|---|---|
| HP | 최대 5. 처음 밟는 안전한 발판마다 +1 회복 |
| 데미지 | 가시 패치·슬라임·천장 가시 각 -1, 피격 후 1.2초 무적(깜빡임) |
| 낙하 데미지 | 착지 없이 190px(약 2개 층) 이상 자유낙하 시 -1, 계속 떨어지면 구간마다 반복 |
| 코인 | 획득 시 점수 +5 |
| 점수 | 깊이(m) + 코인×5. 모드별 최고 기록을 Preferences(`2d-fall`)에 저장 |
| 게임오버 | HP 0, (무한) 천장 위로 밀려남 |

## 4. 난이도 스케일링

`Theme.difficulty(depthM)` = min(깊이/150m, 1). 이 값(diff)으로 전부 보간:

| 항목 | 얕음 → 150m+ |
|---|---|
| (무한) 스크롤 속도 | 32 → 160 px/s |
| (무한) 발판 간격 | 52~76 → +26px |
| (무한) 발판 폭 | 3~6 → 3~5타일 |
| 가시 확률 | 무한 14→30%, 클래식 25→55% |
| 슬라임 확률 | 무한 16→32%, 클래식 20→42% |
| (클래식) 얼음 비율 | 15 → 45% |
| (클래식) 구멍 | 2개·3칸 → 주로 1개·2칸 |
| 슬라임 속도 | 초록 26→52, 보라 48→90 px/s |
| 보라 슬라임 등장 | 40m부터 섞이기 시작, 140m쯤 대부분 보라 |

공정성 보장: 클래식 가시 패치는 세그먼트 전체를 덮지 않는다(항상 안전 칸 1개 이상). 바닥층엔 구멍이 최소 1개 보장된다.

## 5. 테마 (깊이별 분위기)

배경색·벽 틴트는 깊이에 따라 부드럽게 lerp, 타일은 티어 경계에서 교체.

| 티어 | 구간 | 벽/블럭 | 얇은 발판 | 분위기 |
|---|---|---|---|---|
| 0 잔디 | 0~45m | 흙(타일 col1)/잔디 큐브(col0) | 초록(row0) | 남색 배경 |
| 1 사암 유적 | 45~110m | 균열돌(col3)/사암(col2) | 모래색(row1) | 보랏빛, 살짝 탈색 |
| 2 심연 | 110m~ | 어두운 돌(col8) | 주황(row2) | 검붉은 배경, 핏빛 틴트 |

가시 발판 바는 항상 파랑(row3). 페이드 구간: 티어1 45~75m, 티어2 110~150m.

## 6. 조작

| 키 | 동작 |
|---|---|
| ←/→, A/D | 좌우 이동 |
| SPACE/↑/W | 점프 (클래식 전용) |
| SPACE/ENTER | 메뉴 선택·시작 |
| ESC/P | 일시정지 |
| R | (게임오버) 재시작 |
| M | (일시정지·게임오버) 메뉴로 |

## 7. 에셋 사용

- **플레이어**: Mana Seed Character Base Demo 2.0 — base+outfit(fstr)+hair(bob1) 3레이어 페이퍼돌, 64px 프레임을 32px로 축소 렌더. 걷기=페이지1 행6/7, 점프 상승/공중=행0 열6/7.
- **타일·몬스터·코인·사운드·음악·폰트**: Brackeys Platformer Assets.
- **가시·하트**: 팩에 없어서 코드에서 Pixmap으로 절차 생성 (Brackeys 돌 팔레트).
- 라이선스: 루트의 `CREDITS-brackeys.txt`(CC0), `CREDITS-manaseed.txt`(데모 — 상업 배포 전 정식 라이선스 확인 필요).

## 8. 튜닝 상수 위치

전부 `GameWorld.java` 상단: `GRAVITY`, `MAX_FALL(_CLASSIC)`, `MOVE_SPEED(_CLASSIC)`, `JUMP_SPEED`, `FREE_FALL_LIMIT`, `ICE_RESPONSE`, `MAX_HP`. 테마 경계·난이도 램프는 `Theme.java`. 생성 확률은 `GameWorld.makeBar()`/`makeFloor()`.
