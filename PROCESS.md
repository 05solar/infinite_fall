# PROCESS.md — 개발 과정 기록

2026-09-18, gdx-liftoff 템플릿에서 시작해 Claude Code와 함께 반복 개발.

## 진행 순서

1. **에셋 분석**
   - Brackeys 팩과 Mana Seed 데모의 시트를 확대 렌더링해 프레임 좌표를 수동 확정
     (Mana Seed 방향 행 순서 남/북/동/서, Brackeys 슬라임 가운데 행, platforms.png 행 색 순서 등).
   - 팩에 없는 가시/하트는 Pixmap 절차 생성으로 결정.
2. **1차: 무한 모드 단독 버전** — 세로 480×800 창, 자동 스크롤 + 천장 가시 + 얇은 발판 + 슬라임/코인/HP/점수. 전 로직이 Main.java 한 파일.
3. **2차: 모드 분리 + 테마** — 시작 메뉴에서 INFINITE/CLASSIC 선택. 클래식은 두꺼운 지형 발판(당시 2타일). 깊이 3단계 테마(잔디→사암→심연)와 난이도 램프 추가.
4. **3차: 클래식 재설계** — "정해진 길" 컨셉으로 전환: 벽~벽 바닥층 + 구멍 1~2개, 정사각 블럭 1칸 두께, 점프 추가, 얼음(미끄럼) 블럭, 가시를 부분 패치화.
5. **4차: 밸런스 패치** — 클래식 낙하속도 380→230→185, 이동속도 130→100(클래식 전용). 다이빙 악용 방지용 낙하 데미지(190px 초과 자유낙하) 도입 — 처음엔 클래식만, 이후 두 모드 공통.
6. **5차: 구조 정리** — Main 단일 파일을 Assets / Theme / Platform / Slime / GameWorld / GameRenderer / Main 으로 분리. 문서(DESIGN/PROCESS/AGENTS) 작성.

## 검증 방법

- 빌드: `gradlew :core:compileJava`, 실행: `gradlew :lwjgl3:run`.
- 화면 검증은 스크린샷 자동화로 진행:
  - 창이 다른 창에 가려져도 `PrintWindow(hwnd, dc, 3)`으로 창 내용 캡처 가능.
  - 키 입력 주입은 `SetForegroundWindow`+SendKeys가 아니라 `PostMessage(WM_KEYDOWN/UP)`이 GLFW에 먹힘.
    단, 화살표 키는 확장 키 lParam 플래그가 필요해서 W/S/SPACE 같은 일반 키를 사용.
- 각 반복마다: 컴파일 → 실행 → 메뉴/클래식 시작 화면 캡처로 타일·정렬·모드 동작 확인.

## 알게 된 함정들

- 다운로드한 Brackeys zip은 같은 이름 폴더가 이중 중첩되어 있음.
- platforms.png의 행 색 순서는 이미지 눈대중과 달랐음(실제: 초록/모래/주황/파랑) — 런타임 렌더로 확정.
- 클래식 바닥을 두껍게(32px) 하면 층 간 통로가 좁아져 답답함 → 1타일 큐브로 회귀.
- 클래식에서 구멍이 세로로 이어지면 무한 다이빙이 최적해가 됨 → 낙하 데미지로 해결.
